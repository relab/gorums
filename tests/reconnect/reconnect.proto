syntax = "proto3";

package reconnect;

option go_package = "github.com/relab/gorums/tests/reconnect";

import "gorums.proto";

service Reconnect {
  rpc Test(Echo) returns (Echo) { option (gorums.ordered) = true; }
}

message Echo { string Value = 1; }
