// Code generated by protoc-gen-gorums. DO NOT EDIT.
// versions:
// 	protoc-gen-gorums v0.8.0-devel
// 	protoc            v6.30.2
// source: tls/tls.proto

package tls

import (
	context "context"
	gorums "github.com/relab/gorums"
	encoding "google.golang.org/grpc/encoding"
)

const (
	// Verify that this generated code is sufficiently up-to-date.
	_ = gorums.EnforceVersion(8 - gorums.MinVersion)
	// Verify that the gorums runtime is sufficiently up-to-date.
	_ = gorums.EnforceVersion(gorums.MaxVersion - 8)
)

func init() {
	if encoding.GetCodec(gorums.ContentSubtype) == nil {
		encoding.RegisterCodec(gorums.NewCodec())
	}
}

// A TLSConfiguration represents a static set of nodes on which quorum remote
// procedure calls may be invoked.
type TLSConfiguration struct {
	gorums.RawConfiguration
	nodes []*TLSNode
}

// TLSQuorumSpecFromRaw returns a new TLSQuorumSpec from the given raw configuration.
//
// This function may for example be used to "clone" a configuration but install a different QuorumSpec:
//
//	cfg1, err := mgr.NewConfiguration(qspec1, opts...)
//	cfg2 := ConfigurationFromRaw(cfg1.RawConfig, qspec2)
func TLSConfigurationFromRaw(rawCfg gorums.RawConfiguration) (*TLSConfiguration, error) {
	newCfg := &TLSConfiguration{
		RawConfiguration: rawCfg,
	}
	// initialize the nodes slice
	newCfg.nodes = make([]*TLSNode, newCfg.Size())
	for i, n := range rawCfg {
		newCfg.nodes[i] = &TLSNode{n}
	}
	return newCfg, nil
}

// Nodes returns a slice of each available node. IDs are returned in the same
// order as they were provided in the creation of the Manager.
//
// NOTE: mutating the returned slice is not supported.
func (c *TLSConfiguration) Nodes() []*TLSNode {
	return c.nodes
}

// And returns a NodeListOption that can be used to create a new configuration combining c and d.
func (c TLSConfiguration) And(d *TLSConfiguration) gorums.NodeListOption {
	return c.RawConfiguration.And(d.RawConfiguration)
}

// Except returns a NodeListOption that can be used to create a new configuration
// from c without the nodes in rm.
func (c TLSConfiguration) Except(rm *TLSConfiguration) gorums.NodeListOption {
	return c.RawConfiguration.Except(rm.RawConfiguration)
}

// TLSManager maintains a connection pool of nodes on
// which quorum calls can be performed.
type TLSManager struct {
	*gorums.RawManager
}

// NewTLSManager returns a new TLSManager for managing connection to nodes added
// to the manager. This function accepts manager options used to configure
// various aspects of the manager.
func NewTLSManager(opts ...gorums.ManagerOption) *TLSManager {
	return &TLSManager{
		RawManager: gorums.NewRawManager(opts...),
	}
}

// NewTLSConfiguration returns a TLSConfiguration based on the provided list of nodes (required).
// Nodes can be supplied using WithNodeMap or WithNodeList, or WithNodeIDs.
// A new configuration can also be created from an existing configuration,
// using the And, WithNewNodes, Except, and WithoutNodes methods.
func (m *TLSManager) NewConfiguration(cfg gorums.NodeListOption) (c *TLSConfiguration, err error) {
	c = &TLSConfiguration{}
	c.RawConfiguration, err = gorums.NewRawConfiguration(m.RawManager, cfg)
	if err != nil {
		return nil, err
	}
	// initialize the nodes slice
	c.nodes = make([]*TLSNode, c.Size())
	for i, n := range c.RawConfiguration {
		c.nodes[i] = &TLSNode{n}
	}
	return c, nil
}

// Nodes returns a slice of available nodes on this manager.
// IDs are returned in the order they were added at creation of the manager.
func (m *TLSManager) Nodes() []*TLSNode {
	gorumsNodes := m.RawManager.Nodes()
	nodes := make([]*TLSNode, len(gorumsNodes))
	for i, n := range gorumsNodes {
		nodes[i] = &TLSNode{n}
	}
	return nodes
}

// TLSNode holds the node specific methods for the TLS service.
type TLSNode struct {
	*gorums.RawNode
}

// TestTLS is a quorum call invoked on all nodes in configuration c,
// with the same argument in, and returns a combined result.
func (n *TLSNode) TestTLS(ctx context.Context, in *Request) (resp *Response, err error) {
	cd := gorums.CallData{
		Message: in,
		Method:  "tls.TLS.TestTLS",
	}

	res, err := n.RawNode.RPCCall(ctx, cd)
	if err != nil {
		return nil, err
	}
	return res.(*Response), err
}

// TLS is the server-side API for the TLS Service
type TLSServer interface {
	TestTLS(ctx gorums.ServerCtx, request *Request) (response *Response, err error)
}

func RegisterTLSServer(srv *gorums.Server, impl TLSServer) {
	srv.RegisterHandler("tls.TLS.TestTLS", func(ctx gorums.ServerCtx, in *gorums.Message, finished chan<- *gorums.Message) {
		req := in.Message.(*Request)
		defer ctx.Release()
		resp, err := impl.TestTLS(ctx, req)
		gorums.SendMessage(ctx, finished, gorums.WrapMessage(in.Metadata, resp, err))
	})
}
