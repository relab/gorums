syntax = "proto3";

package tls;

option go_package = "github.com/relab/gorums/tests/tls";

import "gorums.proto";

service TLS {
  rpc TestTLS(Request) returns (Response) { option (gorums.ordered) = true; }
}

message Request {}

message Response { bool OK = 1; }
